import styled from 'styled-components'

export const Header = () => {
  return <HeaderStyle />
}

const HeaderStyle = styled.header`
  position: sticky;
  top: 0;
  width: 100vw;
  background-color: #0572ff;
  height: 10vh;
`
