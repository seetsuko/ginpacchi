'use client'

import styled from 'styled-components'

const HeaderStyle = styled.div`
  background-color: #89bcff;
  height: 120px;
  padding: 30px;
`

export const Header = () => {
  return <HeaderStyle />
}
