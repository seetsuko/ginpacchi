'use client'

import styled from 'styled-components'

export const Header = () => {
  return <HeaderStyle />
}

const HeaderStyle = styled.div`
  background-color: #89bcff;
  height: 120px;
  padding: 30px;
`
