'use client'

import styled from 'styled-components'

export const Header = () => {
  return <HeaderStyle />
}

const HeaderStyle = styled.div`
  background-color: #89bcff;
  height: 10vh;
  padding: 30px;
`
