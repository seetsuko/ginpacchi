'use client'

import styled from 'styled-components'

export const Header = () => {
  return <HeaderStyle />
}

const HeaderStyle = styled.header`
  position: sticky;
  top: 0;
  width: 100vw;
  background-color: #89bcff;
  height: 10vh;
`
